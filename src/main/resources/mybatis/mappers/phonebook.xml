<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="phonebook">
	<resultMap id="resultMapPersonVo" type="com.javaex.vo.PersonVo">
		<result column="person_id" property="personId" />
		<result column="name" property="name" />
		<result column="hp" property="hp" />
		<result column="company" property="company" />
	</resultMap>
		
<!--  쿼리문 작성 --> 

	<!-- 전체 리스트 가져오기 -->
	<select id="selectList" resultType="resultMapPersonVo">
		<![CDATA[
			select  person_id personId,
	        		name,
	        		hp,
	        		company
			from person
			order by person_id desc
		]]>
	</select>
	
	<!-- 전화번호 1명 정보 가져오기 -->
	<select id="selectPerson" parameterType="int" resultType="PersonVo">
		<![CDATA[
			select  person_id,
	        		name,
	        		hp,
	        		company
			from person
			where person_id = #{personId}
		]]>
	</select>


	<!-- 전화번호 1명 정보 가져오기 -->
	<select id="selectPerson2" parameterType="int" resultType="map">
		<![CDATA[
			select  person_id,
	        		name,
	        		hp,
	        		company
			from person
			where person_id = #{personId}
		]]>
	</select>
	
	
	<!-- 전화번호 등록(결과가 없고 저장하면 카운트만 넘어가기때문에 결과타입이 필요없음)-->
	<!-- #{getName/getHp...} -->
	<insert id="insert" parameterType="resultMapPersonVo">
		<![CDATA[
			insert into person
			values (seq_person_id.nextval, 
					#{name},
					#{hp}, 
					#{company})
		]]>
	</insert>
	
	<!-- 전화번호 삭제(받는 값이라 파라미터타입, 주소를 받는게 아니라 값을 받는거라 int)-->
	<delete id="delete" parameterType="int">
		<![CDATA[
			delete from person
			where person_id = #{personId}
		]]>
	</delete>
	
	<!-- 사람정보수정 -->
	<update id="update" parameterType="resultMapPersonVo">
		<![CDATA[
			update person 
			set hp = #{hp},
    			company = #{company}
			where person_id = #{personId}
		]]>
	</update>
	
</mapper>
